1.AppStep -> 기능추가
 -프로그램별 파일2개 : 빅쿼리버전, 오라클버전
 -쿼리 각각 2개 : select * from ~ limit 10; / select count(1) from ~~;
 -(oracle) where rownum < 10;

2.comMapping 에서 기준일자 default는 js지만 화면박스에서 변경되어 있으면 우선순위는 박스의 값을 참조하라

3.trans 에서 날짜계산 로직 추가 

4.오라클에서는 parse_date -> 